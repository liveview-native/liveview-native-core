namespace LiveViewNativeCore {
      ChangeType zoom(ChangeType a);
};
enum ChangeType {
    "Change",
    "Add",
    "Remove",
    "Replace",
};
[Error]
enum ParseError {
      "Reader",
      "Tokenizer",
};

interface Document {
    [Throws=ParseError, Name=parse]
    constructor(string input);
    string to_string();
};
